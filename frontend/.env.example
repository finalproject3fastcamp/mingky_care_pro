# dev 는 vite.config.ts 의 프록시(/api → :8000)를 탄다. 절대 URL 을 넣으면
# 프록시를 지나쳐 CORS 에 막히므로 상대경로로 둔다.
VITE_API_BASE_URL=/api
# 로봇 QR 리더의 MJPEG 미리보기 스트림 (노드 preview_port 와 로봇 IP). 비우면 카메라 카드 숨김
VITE_CAMERA_STREAM_URL=http://192.168.0.22:8080/stream
